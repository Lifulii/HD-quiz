# 知识竞赛刷题程序 · 交接文档

更新日期：2026-09-02（v2：主版本 H5 同步知识点详解与翻题导航）

## 一、这是什么

根据《中国华电集团有限公司青年理论知识网络学习竞赛题库（第二期）》制作的手机刷题网页，7 大部分共 580 题（单选/多选/判断），每题附答案解析和记忆口诀，167 道概念题另有「📚 知识点详解」。无需联网、无需账号，做题记录保存在本机浏览器 localStorage。

本目录里有**两个独立版本**，数据源相同、互不影响：

| 版本 | 入口文件 | 特点 | 适用场景 |
|---|---|---|---|
| 加密分发版（主版本） | `index.html` | 题库加密存放（`data/questions.enc.js`），打开需输访问密码；带收藏夹、错题连对 2 次移出；**v2 起与单文件版功能对齐**：知识点详解、底部翻题导航、题号面板、回看已答题、结果页 | 发给同事多人使用、部署到静态托管 |
| 单文件版 | `刷题.html` | 全部代码+题库+详解在一个 html 里，双击即用；**2026-09-02 新增知识点详解和翻题导航** | 个人离线自用、微信单文件转发 |

主版本的详细使用说明见 `README.md`，本文档侧重整体交接和单文件版的维护。

## 二、文件清单

### 主版本（index.html 体系）
```
index.html            应用入口（含密码验证）
style.css             移动端样式
app.js                应用逻辑
data/questions.js     明文题库（git 忽略，不上传）
data/questions.json   同内容 JSON 版
data/questions.enc.js 加密题库（仓库只存这个）
data/explanations/    解析/口诀源文件 batch_*.json（git 忽略）
data/fulltext.txt、questions_raw.json   提取中间产物（git 忽略）
tools/extract_questions.py   PDF -> questions_raw.json
tools/build_questions.py     合并解析 -> questions.js（含校验）
tools/encrypt_data.js        明文 -> 密文（设置访问密码）
tools/check_app.js           前端引用与数据完整性检查
tools/dump.py                按 id 区间查看题目
```

### 单文件版（刷题.html 体系）
```
刷题.html             单文件应用：样式 + 题库 DATA + 知识点详解 EXD + 逻辑
exd.js                知识点详解源文件（167 题，两个版本共用）
_inject.js            把 exd.js 注入刷题.html 的工具（node 运行）
```

### 其他
```
题库全文.txt          PDF 转出的题库全文（人工核对用）
build/                早期流程的中间产物（git 忽略，可不管）
parse.py              早期解析脚本（git 忽略）
原题库 PDF            不上传仓库
```

## 三、单文件版（刷题.html）数据结构

页面内三个 `<script>` 块：

1. **题库 DATA**：`DATA=[...]`，每题一个数组，字段顺序固定：
   ```
   [题号id, 部分0-6, 题型0单选/1多选/2判断, 题干, [选项...](判断题为[]), 答案字母或"正确/错误", 答案解析, 记忆口诀]
   ```
   部分顺序：`0党的理论 1党史知识 2团青知识 3行业信息 4管理知识 5安全生产 6公文常识`。
2. **知识点详解 EXD**：`EXD={题号:"详解文本", ...}`，`详解文本`中的 `\n` 会渲染成换行。
   该块由 `_inject.js` 从 `exd.js` 自动注入，**不要直接手改 html 里的这块**（下次注入会被覆盖）。
3. **应用逻辑**：`"use strict"` 开头那块。

## 四、2026-09-02 改动内容

### v1（单文件版）
1. **新增「📚 知识点详解」**：为 167 道概念性题目补写了讲透知识点的详解（不再只是复述答案），覆盖：
   - 安全生产（501-540）全部 40 题——如 510 题完整讲解 LEC 评价法（L 事故发生可能性 / E 暴露频繁程度 / C 后果严重度，D=L×E×C 分级表）；
   - 公文常识（541-580）全部 40 题（导语体系、公文格式、文种辨析等）；
   - 行业信息、管理知识、团青知识中的概念题，及少量党的理论概念题（如五位一体、四个全面）。
   - 作答后在「答案解析」下方以紫色板块显示；无详解的题不显示该板块。
2. **底部固定导航栏**：`‹ 上一题`、`☰ 题号`、`下一题 ›`（最后一题显示「查看结果」）。
3. **题号选择面板**：点「☰ 题号」弹出当前练习的题号网格，绿=答对、红=答错、灰=未答、红框=当前题，点题号直接跳转。
4. **回看已答题**：跳回已答过的题会还原当时的作答状态和解析板块（会话内记录 `S.ans` / `S.orders`，选项乱序位置也保持一致）。

界面结构调整：答题区底部留出导航栏高度（`.qwrap`），原来答完题后的「返回首页/下一题」按钮行已移除，改由底部导航栏承担。

### v2（主版本 H5 同步对齐）
1. **知识点详解进主版本**：`tools/build_questions.py` 现在会解析根目录 `exd.js`（逐行 `题号:"内容"` 格式，还原 `\n` 换行），把详解写入题目的 `detail` 字段，随 `questions.js/json` 一起加密进 `questions.enc.js`。两版共用同一个 `exd.js` 源文件，不会分叉。
2. **底部导航栏**：`‹ 上一题`、`☰ 题号`、`下一题 ›`（最后一题为「查看结果」），进入新增的结果页（正确率/完成/答对/答错 + 返回首页/重练错题）。
3. **题号选择面板**：底部弹层题号网格，绿=答对、红=答错、灰=未答、红框=当前题，点题号跳转。
4. **回看已答题**：会话内 `state.ans` 记录每题作答（显示字母+对错），跳回已答题还原标色和解析/详解板块。
5. **打乱选项顺序开关**：首页设置卡片新增开关（localStorage `hdq_shuffle_v1`，默认开），选择题 ABCD 随机排列、判断题不受影响；会话内 `state.orders` 记录每题选项映射，作答判定先换算回原始字母，回看时选项位置保持一致。出题顺序（顺序/随机）也从原 mode-bar 并入该设置卡片。

## 五、日常维护操作手册

### A. 改单文件版的题目/解析/口诀
直接用编辑器打开 `刷题.html`，搜题号（如 `[510,`）改 DATA 里对应字段。注意保持数组字段顺序和引号转义。改完用浏览器打开抽查即可。

### B. 改/补「知识点详解」（两版共用 exd.js）
1. 编辑 `exd.js`（按 `题号:"内容"` 增改，每条一行，`\n` 换行，内容里不要出现 `</script>`）；
2. 单文件版：运行 `node _inject.js`（先校验 exd.js 语法，再整体替换 html 中的 EXD 块）；
3. 主版本：`python tools/build_questions.py`（会校验 exd.js 每行可解析、题号无越界），再 `node tools/encrypt_data.js "访问密码"` 重新加密；
4. 浏览器分别打开 `刷题.html` 和 `index.html`，找对应题号作答抽查。

### C. 改主版本的解析/口诀（README 也有）
1. 改 `data/explanations/batch_*.json` 对应题号；
2. `python tools/build_questions.py` 重新生成 `data/questions.js`；
3. `node tools/encrypt_data.js "访问密码"` 重新生成密文；
4. `node tools/check_app.js` 校验后提交。

### D. 验证
- 主版本：`node tools/check_app.js`（DOM id 引用 + 题目内容完整性）；`python tools/build_questions.py` 自带详解合并校验。
- 单文件版：`node _inject.js` 自带 exd.js 语法校验；另可用 node 对 html 中三个 script 块做 `new Function()` 语法检查（已验证通过：DATA 580 题、EXD 167 条、题号无越界）。

## 六、注意事项

- **两套记录不互通**：主版本和单文件版各用各的 localStorage 键（单文件版为 `hdquiz_v1`）；换浏览器/换设备记录也不互通，清浏览器数据会丢记录。
- **访问密码**只用于主版本，通过加密工具设置，不要写进仓库或文档。
- 解析、口诀、知识点详解均为 AI 辅助生成并经抽查，个别表述请以官方文件原文为准；发现问题按题号反馈修正（走上面 A/B/C 流程）。
- `exd.js` 是两版共用的详解源文件：`刷题.html` 靠 `_inject.js` 注入，主版本靠 `build_questions.py` 合并进题库数据，改完两边都要重新生成。
- 题号跳转/上一题只看当前练习会话（全部题库/某部分/错题本/收藏夹），跳题作答后错题本会按最新结果实时更新。
